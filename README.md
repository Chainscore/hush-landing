# hush landing page

Modern static Next.js landing page for Hush, built from the Figma design and deployed to GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The build is exported to `out/` for GitHub Pages. The custom domain is configured through `public/CNAME`.
